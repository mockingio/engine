project_name: engine

before:
  hooks:
    - go mod tidy

builds:
  - skip: true

release:
  github:
    owner: mockingio
    name: mockingio

changelog:
  use: github
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'